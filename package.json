{
  "name": "csc437",
  "version": "1.0.0",
  "description": "web dev class cal poly",
  "main": "index.html",
  "scripts": {
    "start": "cd packages/proto && http-server dist -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
      "packages/*"
  ],
  "dependencies": {
    "http-server": "^14.1.1"
  }
}