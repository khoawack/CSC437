{
  "name": "csc437",
  "version": "1.0.0",
  "description": "web dev class cal poly",
  "main": "index.html",
  "scripts": {
    "build": "cd packages/proto && npm run build",
    "start": "npm run build && cd packages/server && npm run start:proto",
    "dev:server": "cd packages/server && npm run dev",
    "dev:client": "cd packages/proto && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
      "packages/*"
  ],
  "dependencies": {
    "http-server": "^14.1.1"
  }
}